% Optional Week-2 experiment — Packet Loss Robustness (eval-only on frozen ckpts)
% Not part of Week-1 v1 PDF unless completed.
%
% Usage:
%   python scripts/eval_packet_loss.py --seed 42 --episodes 64 --plot
%
% Intentionally deferred for RA-L if Time; main story remains communication efficiency.

\documentclass{article}
\begin{document}
Placeholder. Real runner: \texttt{scripts/eval\_packet\_loss.py}.
\end{document}
