# Abstract Revision Notes (v0.11)

## Must include

1. **Problem** — graph treated as fixed infrastructure under resource limits  
2. **Formulation** — topology as learnable coordination variable \(G_t=\phi_\theta(s_t)\)  
3. **Theory** — budget feasibility + shared-state discrepancy bounds + complexity prop.  
4. **Experiments** — feasibility, twin evidence, scaling, channels, \((J,C)\) baselines  

## Must keep (core positioning)

> topology is a learnable coordination variable

## Must avoid

- performance generalization / “scalable to large swarms”  
- “robust communication guarantee”  
- “optimal topology”  
- “outperforms all baselines”

## v0.11 abstract policy

Keep the v0.10 scientific content; tighten title-level “scalable” elsewhere; ensure theory verbs stay “establish / bound / characterize” and empirics stay “consistent with / empirically observe.”
