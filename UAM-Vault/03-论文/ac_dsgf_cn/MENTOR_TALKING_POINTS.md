# 见导师：三分钟口径（AC-DSGF）

## 先认账（30 秒）

老师指出的主表与诊断表 Success/Comm 打架，**成立**。  
根因不是“随机波动”，而是：

1. 主表 rollout **漏了** TorchRL 的 `step_mdp` → Success 被系统性压到 ~4%；  
2. 主表 Comm 取的是**训练日志均值**，不是终评门控质量 → 与诊断 Comm≈0.008 对不上。

## 已做（30 秒）

- 已修 `utils/eval_rollout.py`（补 `step_mdp` + 终评 Comm）；  
- 冒烟：同一 s42 ckpt，AC Success **23.4%**、Comm **0.0077**（与诊断同量级）。

## 贡献怎么讲（60 秒）

> 我们做的是**通信半径约束下的可学习门控 + 预算**，让蜂群在有限链路上稀疏交互。  
> 不是突破半径的任意组网，也还没有独立的 pairwise 任务关联度 \(r_{ij}\)；  
> 任务信息主要通过节点观测嵌入进入门控。核心卖点是**通信—性能权衡**，不是刷最高 Success。

## 下一步（30 秒）

1. 冻结权重，**统一协议重评** 5 方法 × 5 seeds × 200-ep；  
2. 重写表 1 / 英文 RA-L 数字；  
3. 再决定要不要调 \(\lambda_c\) 或补任务边特征——**先重评，再重训**。

## 不要说的话

- “主张锁定 / 非声称 / 不复活 ++”  
- “4% 已经保持性能”  
- “我们是任务关联度 \(r_{ij}\) 驱动通信”（当前代码不支持）
