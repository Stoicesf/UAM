# Highlights — AC-DSGF v1（final）

### H1
Topology elevated from fixed infrastructure to a soft-budget decision variable: *when and with whom to communicate*.

### H2
AC-DSGF: differentiable sparsity learning (Candidate Edge Scoring → Budget-Constrained Edge Selection → Residual Recovery).

### H3
Theory as interpretation: degree-constraint scaling \(\eta_N=\mathcal{O}(1/N)\); residual *action* stability; gating as *approximate/induced* budgeted edge selection (not an explicit solver).

### H4
Comparable coordination under identical / hard budgets; primary gain is substantially lower SCA (soft activation), not “best Top-K neighbors.”
