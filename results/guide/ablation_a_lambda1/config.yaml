# Ablation A — obs+Phi, lambda=1.0 (failed regime reproduction)
experiment:
  id: ablation_a
  stage: 1
  name: "Ablation A: lambda=1.0"

train:
  seed: 42
  total_frames: 10_240
  algorithm: guided_mappo

guidance:
  mode: mlp
  guidance_dim: 4
  hidden_dim: 128
  use_guidance_reward: true
  guidance_reward_coef: 1.0
  guidance_warmup: false

output:
  category: guide
  results_dir: results
